const MastergridSearchPanel = () => {
    return (
        <>
            {/* {searchPanel && searchFields ? (
                <SearchComponent
                    callFrom='master'
                    searchConfig={searchPanel}
                    setQuery={handleSearch}
                    defaultSearchValues={rest.defaultSearchValues}
                    defaultSearchItems={rest.defaultSearchItems}
                />
            ) : null} */}
        </>
    );
};

export default MastergridSearchPanel;
